import { numberThousandFormat } from '../../../helpers/number-thousand-format';
import { module, test } from 'qunit';

module('Unit | Helper | number thousand format');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = numberThousandFormat(42);
  assert.ok(result);
});
