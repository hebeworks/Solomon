import { statnoticeTypeIcon } from '../../../helpers/statnotice-type-icon';
import { module, test } from 'qunit';

module('Unit | Helper | statnotice type icon');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statnoticeTypeIcon(42);
  assert.ok(result);
});
