import { deCamelize } from '../../../helpers/de-camelize';
import { module, test } from 'qunit';

module('Unit | Helper | de camelize');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = deCamelize(42);
  assert.ok(result);
});
