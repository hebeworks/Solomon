import { largeNumberFormatter } from '../../../helpers/large-number-formatter';
import { module, test } from 'qunit';

module('Unit | Helper | large number formatter');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = largeNumberFormatter(42);
  assert.ok(result);
});
