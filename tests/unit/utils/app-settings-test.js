import appSettings from '../../../utils/app-settings';
import { module, test } from 'qunit';

module('Unit | Utility | app settings');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = appSettings();
  assert.ok(result);
});
