import solomonUtils from '../../../utils/solomon-utils';
import { module, test } from 'qunit';

module('Unit | Utility | solomon utils');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = solomonUtils();
  assert.ok(result);
});
