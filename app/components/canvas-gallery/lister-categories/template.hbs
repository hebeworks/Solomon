<section class="gallery__section -categories">
	{{#each categories as |cat|}}
		<h3 class="thumb-list-heading float--left">{{cat.category.title}}</h3>
		<!--<a href="#" class="link -light float--right">See all ></a>-->
		<div class="cf"></div>
		<div class="js-thumb-list thumb-list">
			<ul class="thumb-list__inner">
				{{#each cat.items as |canvas|}}
					{{!-- stories/story-library/list-view-full-width-description model=canvas --}}
					{{stories/story-library/list-view-thumbnail model=canvas action="loadACanvas" thumbIcon="-add-canvas"}}
				{{/each}}
			</ul>
		</div>
	{{/each}}
	{{yield}}
</section>